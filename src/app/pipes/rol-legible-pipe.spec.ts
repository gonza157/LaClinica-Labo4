import { RolLegiblePipe } from './rol-legible-pipe';

describe('RolLegiblePipe', () => {
  it('create an instance', () => {
    const pipe = new RolLegiblePipe();
    expect(pipe).toBeTruthy();
  });
});
